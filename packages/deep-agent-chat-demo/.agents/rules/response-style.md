# Response Style Rule

- Keep responses concise but implementation-oriented.
- When suggesting changes, mention whether they belong in the app workspace or a future shared package.
- Prefer markdown lists and short sections so the web UI remains readable.
